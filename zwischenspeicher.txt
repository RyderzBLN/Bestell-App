<svg xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 -960 960 960" width="24px" fill="#FF8000">
  <path d="M200-440v-80h560v80H200Z"/>
</svg>



function addCart(index){
  cart.push(steakdishes[index]);
  steakdishes[index].quantity += 1;
  renderCartArticel();
  
  console.log(steakdishes[index].quantity)
}



function addCart(index){
  let steak = steakdishes[index];
  let cartIndex = cart.findIndex(cartItem => cartItem.name === steak.name);
  console.log(cartIndex);

  if (cartIndex){
    steakdishes[index].quantity += 1;
    renderCartArticel();
  } else {
  cart.push(steakdishes[index]);
  steakdishes[index].quantity += 1;
  renderCartArticel();
   }
}



function addCart(index){
  cart.push(steakdishes[index]);
  renderCartArticel();
}




function addPrice(index){
  let numbers = [25.99, 34.99, 29.99, 22.99, 45.99]

  let steak = steakdishes[index];
  let moreSteak = steakdishes[index].quantity; 
  let calcSteak = moreSteak += 1;
  let addPrice = numbers[index];
  
  
 
  if (calcSteak){
    steak.price += addPrice;
    steakdishes[index].quantity += 1; 
    renderCartArticel();
  }
}




//// zuletzt funktioniert 
let steakdishes = [
  {
    "name": "Ribeye Steak",
    "description": "Zartes Ribeye-Steak mit feiner Marmorierung, gegrillt auf offener Flamme und serviert mit Knoblauchbutter.",
    "price": 25.99,
    "quantity": 0
  },
  {
    "name": "Filet Mignon",
    "description": "Ein butterzartes Filet Mignon, perfekt medium-rare zubereitet, begleitet von einer Rotweinreduktion.",
    "price": 34.99,
    "quantity": 0
  },
  {
    "name": "T-Bone Steak",
    "description": "Saftiges T-Bone-Steak, das die besten Teile des Filets und des Roastbeefs vereint, serviert mit einer Pfeffersoße.",
    "price": 29.99,
    "quantity": 0
  },
  {
    "name": "Sirloin Steak",
    "description": "Gut gereiftes Sirloin-Steak, gegrillt nach Wunsch und serviert mit einer hausgemachten Kräuterbutter.",
    "price": 22.99,
    "quantity": 0
  },
  {
    "name": "Tomahawk Steak",
    "description": "Ein imposantes, extra großes Tomahawk-Steak, saftig und reich an Geschmack, serviert mit gerösteten Gemüsebeilagen.",
    "price": 45.99,
    "quantity": 0
  }
];


let cart = [];   


function renderFood(food){
    let contentRef = document.getElementById("food-content");
    contentRef.innerHTML = "";

    for (let i = 0; i < food.length; i++) {
        contentRef.innerHTML += orderTemplate(food[i], i);        
    }
}


function availableInCart(index){
  let numbers = [25.99, 34.99, 29.99, 22.99, 45.99];
  let steak = steakdishes[index]
  let isInCart = cart.find(dish => dish.name === steak.name)

  if (isInCart){
    steak.quantity += 1;   
    steak.price += numbers[index];
  } else {
    steak.quantity = 1;
    cart.push(steak);    
  }
  
}


function addCart(index){
  availableInCart(index);
  renderCartArticel();
}

function morePrice(index) {
  let numbers = [25.99, 34.99, 29.99, 22.99, 45.99];
  let steak = steakdishes[index];
  let more = steak.quantity += 1;

  if (more) {     
      steak.price += numbers[index];
  }
}


function addPrice(index){
  morePrice(index);
  renderCartArticel();
}


function deleteFromCart(index) {
  let numbers = [25.99, 34.99, 29.99, 22.99, 45.99];
  let steak = steakdishes[index];  
  steak.quantity = 0;
  steak.price = numbers[index];
  cart.splice(index, 1);
  renderCartArticel(); 
}


function renderCartArticel() {
  let cartRef = document.getElementById("shopping-cart");
  cartRef.innerHTML = ""; 

  for (let i = 0; i < cart.length; i++) {
    cartRef.innerHTML += cartTemplate(cart[i], i);
  }
}


